# 小書樓操作說明

> 最後更新：2026-06-02

---

## 一、發布新章節（最常用）

### 步驟

**1. 拉取最新版本**
```bash
cd /home/jasper/workspace/thesowordcom
git pull origin main
```

**2. 建立章節檔案**

在 `soword/src/content/chapters/` 新增一個 `.md` 檔案。

檔名格式：`{作品slug}-ch{三位數}.md`

範例：`silent-echo-ch010.md`、`silent-echo-ch020.md`

**3. 填入內容**

```markdown
---
title: "第一章 潮聲"
work: "silent-echo"
chapter: 10
wordCount: 3842
publishedAt: 2026-06-02
---

正文從這裡開始……
```

| 欄位 | 說明 | 必填 |
|------|------|:----:|
| `title` | 章節標題（顯示用） | ✓ |
| `work` | 所屬作品的 slug | ✓ |
| `chapter` | 章節序號，建議用 10、20、30…（方便日後插章） | ✓ |
| `wordCount` | 本章字數（手填） | |
| `publishedAt` | 發布日期，格式 YYYY-MM-DD | ✓ |
| `updatedAt` | 修改日期（只有修改時才加） | |

**4. 提交並推送**
```bash
git add soword/src/content/chapters/silent-echo-ch010.md
git commit -m "feat(content): 新增《作品名》第一章「章節名」"
git push origin main
```

**5. 等待部署**

約 3–5 分鐘後，前往 https://thesoword.com 確認章節出現。

可以在 https://github.com/mayoruna06/thesowordcom/actions 即時查看進度。

---

## 二、新增整部作品

**1. 建立作品介紹頁**

新增資料夾：`soword/src/content/works/{作品slug}/`

在資料夾內建立 `index.md`：

```markdown
---
title: "沉默的回聲"
genre: "romance"
status: "ongoing"
summary: "退役偵探林硯試圖在海邊小城重建平靜生活……（200字以內）"
cover: "silent-echo.jpg"
tags:
  - "BL"
  - "現代"
  - "懸疑"
featured: false
publishedAt: 2026-06-02
---

## 關於這部作品

在這裡寫作品說明……

## 更新時間

每週日晚上十點更新。
```

**genre 可填值：** `romance`（言情）、`fantasy`（奇幻）、`sci-fi`（科幻）、`thriller`（懸疑）、`slice-of-life`（日常）、`other`

**status 可填值：** `ongoing`（連載中）、`completed`（完結）、`hiatus`（暫停）、`draft`（草稿，不對外顯示）

**2. 放封面圖（選填）**

將封面圖放入 `soword/public/images/covers/`，檔名對應 `cover` 欄位填的值。

建議尺寸：寬 600px，JPEG 格式，檔案控制在 200KB 以內。

**3. 新增第一章**

同「發布新章節」步驟，`work` 填剛才建立的作品 slug。

**4. 推送**
```bash
git add soword/src/content/works/silent-echo/
git add soword/src/content/chapters/silent-echo-ch010.md
git add soword/public/images/covers/silent-echo.jpg  # 如果有封面
git commit -m "feat(works): 新增《沉默的回聲》+ 第一章"
git push origin main
```

---

## 三、修改已發布章節

**1. 開啟並修改檔案**

在 `soword/src/content/chapters/` 找到對應的 `.md` 檔案，直接編輯。

**2. 更新 `updatedAt` 欄位**

```markdown
---
title: "第一章 潮聲"
work: "silent-echo"
chapter: 10
publishedAt: 2026-06-02
updatedAt: 2026-06-10   ← 加上今天的日期
---
```

**3. 推送**
```bash
git add soword/src/content/chapters/silent-echo-ch010.md
git commit -m "fix(content): 修正《沉默的回聲》第一章錯字"
git push origin main
```

---

## 四、用 GitHub 網頁直接發文（沒有電腦時）

1. 前往 https://github.com/mayoruna06/thesowordcom/tree/main/soword/src/content/chapters
2. 點右上角 **Add file → Create new file**
3. 檔名欄填入：`silent-echo-ch010.md`
4. 內容貼入完整 Markdown（含 frontmatter）
5. 滾到下方，Commit message 填：`feat(content): 新增第一章`
6. 選 **Commit directly to main** → 點 **Commit changes**

部署會自動觸發，同樣約 3–5 分鐘上線。

---

## 五、本地預覽

```bash
cd /home/jasper/workspace/thesowordcom/soword
pnpm run dev
# 開啟 http://localhost:4321
```

修改後瀏覽器會自動熱重載，不需重新啟動。

---

## 六、Commit Message 格式

```
feat(content):  新增章節或作品
fix(content):   修正錯字 / 排版
style(layout):  調整版面樣式
chore:          更新套件、維護
```

---

## 七、緊急排錯

| 問題 | 解法 |
|------|------|
| 部署後畫面沒更新 | 強制重整：Windows `Ctrl+Shift+R`，Mac `Cmd+Shift+R` |
| Actions 失敗（紅叉） | 前往 Actions 頁面看錯誤訊息，常見原因：frontmatter 格式錯誤 |
| 新作品不顯示 | 確認 `status` 不是 `draft` |
| 章節順序亂掉 | 確認同一作品的 `chapter` 數字沒有重複 |
| 想清除全站 CDN 快取 | Actions → Deploy 小書樓 → Run workflow → 勾選「清除全站快取」 |

---

## 八、網址對照

| 頁面 | 網址 |
|------|------|
| 首頁 | https://thesoword.com |
| 作品 X 介紹頁 | https://thesoword.com/works/{作品slug}/ |
| 作品 X 第一章 | https://thesoword.com/works/{作品slug}/{作品slug}-ch010/ |
| RSS 訂閱 | https://thesoword.com/rss.xml |
| GitHub Repo | https://github.com/mayoruna06/thesowordcom |
| Actions 部署紀錄 | https://github.com/mayoruna06/thesowordcom/actions |
